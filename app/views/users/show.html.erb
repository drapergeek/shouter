<h1><%= @user.email %></h1>
<div id="shouts">
  <%= render @shouts %>
  <% if can_follow  @user %>
    <%= button_to "Follow", user_following_relationships_path(@user), class: "btn btn-warning" %>
  <% end %>
</div>
